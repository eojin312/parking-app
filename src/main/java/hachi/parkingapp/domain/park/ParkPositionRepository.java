package hachi.parkingapp.domain.park;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * parkPositionRepository
 */
public interface ParkPositionRepository extends JpaRepository<ParkPosition, Object> {
}
